# Displays Long List
ll

# Creates file and populates with history
history > moving_around.txt

# Clears screen
clear

# Displays Long List
ls -l

# Displays manual section for less
man less

# Pipes History to Less
history | less

# Displays History
history

# Changes to home directory
~

# Displays list
ls

# Displays list for one directory level higher
ls ..

# Displays list for home directory
ls ~

# Changes directory to one level up
..

# Prints working directory
pwd

# Changes directory to "Assistants"
cd Assistants/

# Changes directory to "Keyboard Layouts" forward slash ignores the following character. In this case the space.
cd Keyboard\ Layouts/

# Changes to home directory
cd ~

# Displays the section of the manual pertaining to ls
man ls

# Change Directory
cd

# Display git version
git --version

# Updates brew
brew update

# Sets config for individual computer
git config --global user.email 'jallengerber@gmail.com'
git config --global user.name 'Jeff Gerber'

# Creates file moving_around.txt and populates file with history.
Use > to overwrite info. Use >> to Cacotonate info. In existing file.
history > moving_around.txt

# Clear terminal screen and move prompt to top.
clear

# Display Long List
ls -l

# Display manual section for less
man less

# Takes the output form history and uses it as input for less. Pipeing.
history | less

# Displays history
history

# Changes to home directory
~

